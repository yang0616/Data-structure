#include "Stack.h"
#include "Queue.h"

int main()
{
/*	//≤‚ ‘∂”¡–
	Queue queue;
	InitQueue(&queue);
	ElemType res;
	for( int i = 0; i < 5; i++)
	{
		PushQueue(&queue, i * 10);
		Show(&queue);
	}

	if(PopQueue(&queue, &res))
	{
		printf("res = %d\n", res);
	}
	Show(&queue);



	//≤‚ ‘’ª
	Stack stack;
	InitStack(&stack);
	ElemType res = 0;
	for( int i = 0; i < 10; i++)
	{
		PushStack(&stack, i);
	}	
	if(PopStack(&stack, &res))
	{
         printf("the top of stack is %d\n", res);
	}

*/	return  0;
}